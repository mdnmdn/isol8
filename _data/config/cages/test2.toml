# isol8 cage — generated by `isol8 @cage new` / `@cage edit` (Phase 8)
schema = 1
name = "test2"
home = "@managed/test2"

# profiles = [] → config default_profiles apply at run time
profiles = []

# isol8:managed — rewritten by `@cage edit`
[toolchains."integrations/gh-cli"]
strategy = "isolate"

# isol8:managed — rewritten by `@cage edit`
[toolchains."integrations/git-identity"]
strategy = "isolate"

# isol8:managed — rewritten by `@cage edit`
[toolchains.bun]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.cargo]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.dotnet]
strategy = "share"

# isol8:managed — rewritten by `@cage edit`
[toolchains.flutter]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.foundry]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.go]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.gradle]
strategy = "share"

# isol8:managed — rewritten by `@cage edit`
[toolchains.homebrew]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.maven]
strategy = "share"

# isol8:managed — rewritten by `@cage edit`
[toolchains.mise]
strategy = "isolate"

# isol8:managed — rewritten by `@cage edit`
[toolchains.npm]
strategy = "share"

# isol8:managed — rewritten by `@cage edit`
[toolchains.nvm]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.pnpm]
strategy = "share"

# isol8:managed — rewritten by `@cage edit`
[toolchains.pyenv]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.python-uv]
strategy = "share"

# isol8:managed — rewritten by `@cage edit`
[toolchains.sdkman]
strategy = "link"

# isol8:managed — rewritten by `@cage edit`
[toolchains.wasmer]
strategy = "link"

# user-owned dirs (wizard preserves these on edit):
[[dirs]]
path = "/Users/mdn/works/projects/agent-manager/workspace/isol8"
access = "rw"

