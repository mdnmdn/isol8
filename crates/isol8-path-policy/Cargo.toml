[package]
name = "isol8-path-policy"
version = "0.1.0"
edition = "2021"
description = "Portable path-grant policy matching for isol8 (Windows hook DLL + engine)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"