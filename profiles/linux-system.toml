# linux-system — backward-compatible alias for linux/system-runtime.
#
# Older invocations use `--profile linux-system`. This thin layer preserves that
# name while delegating to the namespaced Linux runtime.

requires = ["linux/system-runtime"]